FROM {DOCKER_KAFKA_IMAGE_NAME}:{DOCKER_KAFKA_IMAGE_TAG}

ENV KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:{DOCKER_KAFKA_PORT}
ENV KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://localhost:{DOCKER_KAFKA_PORT}
ENV KAFKA_ZOOKEEPER_CONNECT={DOCKER_ZOOKEEPER_IP}:{DOCKER_ZOOKEEPER_PORT}

EXPOSE {DOCKER_KAFKA_PORT}